# Environment variables for database build process
# Copy to .env and fill in values

# No API keys needed for database build (all sources are open/CC-licensed)
# This file exists as a template per project security rules